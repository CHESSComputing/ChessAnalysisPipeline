"""This subpackage contains generic utilities for fitting, parsing
CHESS scan data, collecting interactive user input, and finding
lattice properties of materials (among others).

Submodules summary
------------------

converters
    Functions for converting between some commonly used data formats.

fit
    Generic curve fitting module.

general
    A collection of generic functions for use in any CHAP Processor,
    Reader or Writer.

material
    Module defining the Material class.

models
    Utils `Pydantic <https://github.com/pydantic/pydantic>`__ model
    classes.

parfile
    Utilities for interacting with scans using an SMB-style par file
    as input.
"""

#from CHAP.utils.fit import FitProcessor
